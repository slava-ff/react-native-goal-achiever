import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const WhyDescription = ({goalUnit}) => {
  return (
    <View>
      <Text>Why do I want it?</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default WhyDescription;
